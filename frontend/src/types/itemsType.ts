export type StoreItems = {
  itemId: string;
  itemName: string;
  description: string;
  price: number;
}[];

export type MyItems = {
  itemId: string;
  itemName: string;
  quantity: number;
  description: string;
}[];
