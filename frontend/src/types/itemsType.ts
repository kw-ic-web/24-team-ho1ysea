export type MyItems = {
  itemId: string;
  itemName: string;
  image: string;
  quantity: number;
  description: string;
}[];

export type BuyItem = {
  message: string;
};

export type SellItem = {
  message: string;
};
